# The `conformance/` directory list. It holds one `include()` of the
# registration list beside it. The generated ColdFire conformance corpus and
# its runner register their names here rather than in `tests/tests_cpu.cmake`.

include(conformance_cpu.cmake)
